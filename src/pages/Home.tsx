import { Outlet } from "react-router-dom";
import ToDoLayout from "../components/ToDo/ToDoLayout";

const Home = () => {
  return (
    <div>
      hello word!
    </div>
  );
};

export default Home;
